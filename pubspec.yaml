name: inner_circle
description: "A new Flutter project."
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.4.3 <4.0.0"

# --- قسم الاعتماديات الأساسية للتطبيق ---
dependencies:
  cloud_firestore: ^5.0.1 # يجب أن يكون إصدار متوافق

  # Icons
  cupertino_icons: ^1.0.6
  firebase_auth: ^5.1.0 # يجب أن يكون إصدار متوافق

  # Firebase Services
  firebase_core: ^3.1.0 # يجب أن يكون إصدار متوافق
  firebase_messaging: ^16.0.0
  firebase_storage: ^13.0.0
  flutter:
    sdk: flutter

  # State Management
  flutter_riverpod: ^2.5.1 # مكانه الصحيح هنا
  intl: ^0.20.2
# --- قسم اعتماديات التطوير فقط ---
dev_dependencies:
  flutter_lints: ^3.0.0
  flutter_test:
    sdk: flutter
  flutterfire_cli: ^0.2.7
  uuid: ^4.5.1

flutter:
  uses-material-design: true
